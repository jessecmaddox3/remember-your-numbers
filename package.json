{
  "name": "remember-your-numbers",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "build": "node scripts/build.mjs",
    "start": "node scripts/serve.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
